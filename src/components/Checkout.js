import React from "react";
import CheckoutForm from "../routes/CheckoutForm"; // Adjust the path to where your CheckoutForm is located

const Checkout = () => {
    return (
        <div>
            <h1>Checkout</h1>
            <CheckoutForm />
        </div>
    );
};

export default Checkout;
